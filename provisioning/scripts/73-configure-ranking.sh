#!/bin/bash -ex
# -e: termina si algún pipeline falla
# -x: escribe los comandos y argumentos según los va ejecutando

# En este paso se puede configurar el ranking para añadir en él
# las fotos de participantes y banderas de los equipos.
# El directorio del ranking depende de la instalación, por lo que
# el script está en cada directorio específico
